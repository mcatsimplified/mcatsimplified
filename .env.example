# Copy this file to `.env` and fill in your own keys.
# `.env` is gitignored — never commit it.

# ElevenLabs API key — required for video-use (filler removal, transcription)
# Get one at: https://elevenlabs.io/app/settings/api-keys
# Also copy to ~/Developer/video-use/.env
ELEVENLABS_API_KEY=

# ClickUp API key — only needed if a project integrates with ClickUp
CLICKUP_API_KEY=

# OpenAI API key — only needed if a project uses OpenAI for text/image generation
OPENAI_API_KEY=
